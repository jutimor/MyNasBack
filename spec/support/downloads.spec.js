describe('Downloads', () => {
    beforeEach(() => {
        // cache.clear();
    });
    afterEach(() => {
        // cache.clear();
    });
    it('should be true', function () {
        expect(true).toBeTruthy();
    });
});